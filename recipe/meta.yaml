{% set name = "microfilm" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/microfilm-{{ version }}.tar.gz
  sha256: f06a458234a8bc5fec61eacf7523afbccde660e55b940deea58924a27a7b01ad

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.9
    - setuptools-scm
  run:
    - cmap
    - h5py
    - imageio
    - matplotlib
    - matplotlib-scalebar
    - natsort
    - numpy
    - scikit-image
    - tifffile
    - xarray
    - python >=3.9
    - bioio
    - bioio-tifffile
    - imageio-ffmpeg
    - ipywidgets
    - nd2reader

test:
  imports:
    - microfilm
  commands:
    - pip list
    # - pip check
  requires:
    - pip

about:
  home: https://github.com/guiwitz/microfilm
  summary: Creating figures and animations for multi-channel images with a focus on microscopy.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - guiwitz
